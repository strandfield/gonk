enum class EnumTypeIds
{
  FirstTypeId,
{% for t in include.module.types %}
{% if t.enum %}
{{'  '}}{{ t.id }},
{% endif %}
{% endfor %}
{{'  '}}LastTypeId,
};

enum class ClassTypeIds
{
  FirstTypeId,
{% for t in include.module.types %}
{% if t.class %}
{{'  '}}{{ t.id }},
{% endif %}
{% endfor %}
{{'  '}}LastTypeId,
};